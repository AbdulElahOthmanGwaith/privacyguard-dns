# المساهمة في PrivacyGuard DNS

شكراً لاهتمامك بالمساهمة في PrivacyGuard DNS! يوفر هذا الدليل إرشادات وتعليمات للمساهمة في مشروعنا.

## جدول المحتويات

- [قواعد السلوك](#قواعد-السلوك)
- [البدء](#البدء)
- [عملية التطوير](#عملية-التطوير)
- [معايير الترميز](#معايير-الترميز)
- [إرسال التغييرات](#إرسال-التغييرات)
- [الإبلاغ عن المشاكل](#الإبلاغ-عن-المشاكل)
- [الأمان](#الأمان)

---

## قواعد السلوك

يتبع هذا المشروع [قواعد السلوك](CODE_OF_CONDUCT.md) الخاصة بنا. من المتوقع أن تلتزم بهذه القواعد عند المشاركة. يُرجى الإبلاغ عن السلوك غير المقبول إلى [security@privacyguard.dns](mailto:security@privacyguard.dns).

## البدء

### المتطلبات المسبقة

قبل البدء، تأكد من تثبيت ما يلي:

- **Node.js**: الإصدار 18.0.0 أو أعلى
- **npm**: الإصدار 9.0.0 أو أعلى (يأتي مع Node.js)
- **Git**: أحدث إصدار

### إعداد بيئة التطوير

1. **انسخ المستودع (Fork)**

   قم بزيارة [مستودع GitHub](https://github.com/privacyguard/dns) وانقر على زر "Fork" لإنشاء نسختك الخاصة.

2. **استنسخ نسختك**

   ```bash
   git clone https://github.com/YOUR_USERNAME/PrivacyGuard-DNS.git
   cd PrivacyGuard-DNS
   ```

3. **أضف المستودع الأصلي كـ Upstream**

   ```bash
   git remote add upstream https://github.com/privacyguard/dns.git
   ```

4. **ثبت التبعيات**

   ```bash
   npm install
   ```

5. **أنشئ فرع ميزة جديد**

   ```bash
   git checkout -b feature/اسم-الميزة
   ```

## عملية التطوير

### الأوامر المتاحة

| الأمر | الوصف |
|--------|-------------|
| `npm run dev` | بدء خادم التطوير مع إعادة التحميل |
| `npm run build` | بناء ملفات للإنتاج |
| `npm run build:dev` | بناء ملفات التطوير (غير مضغوطة) |
| `npm test` | تشغيل مجموعة الاختبارات |
| `npm run test:watch` | تشغيل الاختبارات في وضع المراقبة |
| `npm run lint` | التحقق من أخطاء التنسيق |
| `npm run lint:fix` | إصلاح أخطاء التنسيق تلقائياً |
| `npm run format` | تنسيق الكود باستخدام Prettier |
| `npm run clean` | حذف ملفات البناء |

### سير عمل التطوير

1. **ابقَ محدّثاً**

   ```bash
   git fetch upstream
   git merge upstream/main
   ```

2. **قم بإجراء التغييرات**

   نفّذ ميزتك أو إصلاحك باتباع معايير الترميز الخاصة بنا.

3. **اختبر تغييراتك**

   ```bash
   npm test
   ```

4. **نفّذ التنسيق والفحص**

   ```bash
   npm run lint
   npm run format
   ```

5. **Commit التغييرات**

   اكتب رسائل واضحة ومفصّلة:

   ```
   feat: إضافة لوحة تكوين خادم DNS جديدة
   
   - إضافة دعم تكوين IPv6
   - تنفيذ مؤشر حالة الخادم
   - تحديث التوثيق للميزات الجديدة
   
   Fixes #123
   ```

6. **ادفع التغييرات**

   ```bash
   git push origin feature/اسم-الميزة
   ```

7. **أرسل Pull Request**

   قم بزيارة نسختك على GitHub وانقر على "New Pull Request".

## معايير الترميز

### HTML

- استخدم عناصر HTML5 الدلالية
- اتبع تسمية BEME لصفوف CSS
- أضف سمات ARIA المناسبة للوصولية
- احتفظ بالسمات بالترتيب الأبجدي

### CSS

- استخدم خصائص CSS المخصصة (المتغيرات) للسمات
- اتبع منهجية BEME للتسمية
- استخدم ميزات CSS الحديثة (Grid, Flexbox, Custom Properties)
- تصميم متجاوب يبدأ من الهاتف
- استخدم أسماء صفحات ذات معنى بالإنجليزية

**مثال:**

```css
:root {
    --primary-color: #0f766e;
    --spacing-md: 1rem;
}

.component-name {
    display: flex;
    gap: var(--spacing-md);
}

.component-name__element {
    color: var(--primary-color);
}

.component-name--modifier {
    border: 2px solid var(--primary-color);
}
```

### JavaScript

- استخدم ميزات ES2022+
- فضّل `const` على `let`، وتجنب `var`
- استخدم دوال السهم للمعاودات
- استخدم async/awayt بدلاً من Promises الخام
- استخدم القوالب النصية لسلسلات الأحرف
- استخدم التفكيك للكائنات والمصفوفات
- أضف تعليقات JSDoc للدوال
- تعامل مع الأخطاء باستخدام try/catch

**مثال:**

```javascript
/**
 * يعالج بيانات المستخدم ويُرجع النتيجة منسقة
 * @param {Object} userData - كائن بيانات المستخدم
 * @param {string} userData.name - اسم المستخدم
 * @param {string} userData.email - بريد المستخدم الإلكتروني
 * @returns {Promise<Object>} كائن المستخدم المنسق
 */
async function processUserData({ name, email }) {
    try {
        const formatted = {
            displayName: name.trim(),
            contact: email.toLowerCase()
        };
        
        return formatted;
    } catch (error) {
        console.error('فشل في معالجة بيانات المستخدم:', error);
        throw error;
    }
}
```

### Git Commits

نتبع مواصفات [Conventional Commits](https://www.conventionalcommits.org/):

| النوع | الوصف |
|------|-------------|
| `feat` | ميزة جديدة |
| `fix` | إصلاح خطأ |
| `docs` | تغييرات التوثيق |
| `style` | تغييرات نمط الكود (تنسيق، إلخ) |
| `refactor` | إعادة هيكلة الكود |
| `perf` | تحسينات الأداء |
| `test` | إضافة أو تعديل الاختبارات |
| `chore` | مهام الصيانة |

**أمثلة على رسائل Commit:**

```
feat(auth): إضافة نظام المصادقة للمستخدمين
fix(dns): حل مشكلة مهلة DNS على Windows
docs(readme): تحديث تعليمات التثبيت
style(css): تنسيق CSS وفقاً لدليل الأسلوب
refactor(i18n): استخراج سلاسل الترجمة إلى JSON
perf(loading): تحسين وقت التحميل الأولي
test(config): إضافة اختبارات وحدة لمحلل التكوين
chore(deps): تحديث webpack إلى الإصدار 5
```

## إرسال التغييرات

### إرشادات Pull Request

1. **العنوان**: استخدم عنواناً واضحاً ومفصّلاً
2. **الوصف**: اشرح ما الذي غيرته ولماذا
3. **لقطات الشاشة**: أضف لقطات شاشة للتغييرات في الواجهة
4. **الاختبار**: وضّح كيف اختبرت تغييراتك
5. **قائمة التحقق**:
   - [ ] يتبع الكود إرشادات الأسلوب الخاصة بنا
   - [ ] تجتاز الاختبارات محلياً
   - [ ] لا توجد أخطاء في التنسيق
   - [ ] تم تحديث التوثيق (إن أمكن)
   - [ ] يتبع الـ Commits التنسيق المتعارف عليه

### عملية المراجعة

1. سيقوم أحد المشرفين بمراجعة PR الخاص بك خلال 48 ساعة
2. قد يُطلب منك إجراء تغييرات
3. بمجرد الموافقة، سيتم دمج PR الخاص بك

## الإبلاغ عن المشاكل

### قبل الإرسال

- ابحث في المشاكل الموجودة لتجنب التكرار
- تحقق مما إذا كان بالإمكان إعادة إنتاج المشكلة
- اختبَر مع أحدث إصدار

### إرسال المشاكل

أضف المعلومات التالية:

1. **الوصف**: وصف واضح للمشكلة
2. **الخطوات لإعادة الإنتاج**: خطوات مفصّلة
3. **السلوك المتوقع**: ماذا يجب أن يحدث
4. **السلوك الفعلي**: ماذا يحدث فعلاً
5. **البيئة**: نظام التشغيل، المتصفح، الإصدار
6. **لقطات الشاشة**: إن أمكن
7. **السجلات**: أي رسائل خطأ ذات صلة

## الأمان

### الإبلاغ عن ثغرات الأمان

إذا اكتشفت ثغرة أمنية، يرجى إرسال بريد إلكتروني إلى [security@privacyguard.dns](mailto:security@privacyguard.dns) بدلاً من فتح مشكلة عامة.

### ممارسات الأمان

- يتم فحص جميع التبعيات بانتظام
- لا يتم أبداً_commit للبيانات الحساسة
- تُدار الأسرار عبر متغيرات البيئة
- يُطلب HTTPS لجميع الاتصالات الخارجية

---

## أسئلة؟

إذا كان لديك أسئلة، لا تتردد في:

- فتح [مناقشة على GitHub](https://github.com/privacyguard/dns/discussions)
- مراسلتنا على [support@privacyguard.dns](mailto:support@privacyguard.dns)
- الانضمام إلى دردشة المجتمع

شكراً للمساهمة في PrivacyGuard DNS! 🛡️
